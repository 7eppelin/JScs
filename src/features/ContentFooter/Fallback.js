import React from 'react'
import styled from 'styled-components/macro'

const Fallback = () => <Div />

const Div = styled.div`
    height: 100%;
    border-top: 34px solid var(--gray5);
`

export default Fallback